# 爱窝啦·货源雷达商品发现修改方案

> 方案日期：2026-08-30（Asia/Shanghai）
> 对应分析：`SUPPLY_RADAR_PRODUCT_DISCOVERY_GAP_ANALYSIS.md`
> 原则：先恢复买家/卖家的决策效率，再做目录归并；不删除商品，不触发 AI 日报，不引入新发布系统。

## 1. 目标

将 `/card-products` 从“后台目录顺序的分组长表”改成“有证据、可解释、可控制的商品发现页”。

目标顺序：

1. 有在售渠道；
2. 核心 AI 商品；
3. 在售渠道覆盖；
4. 最近有效更新时间；
5. 价格和稳定字段兜底。

这个顺序叫“推荐”，不叫“销量”或“真实热度”，因为当前没有成交数据。

## 2. 本轮 P0 实施

### 2.1 独立排序模块

新增 `src/lib/product-ranking.ts`：

- `recommended`：有在售优先 → 核心 AI 产品层级 → 渠道数降序 → 更新时间降序；
- `channels`：有在售优先 → 渠道数降序；
- `price`：有效最低价升序 → 渠道数降序；
- `updated`：有效更新时间降序 → 渠道数降序；
- 所有模式最后使用平台后台顺序、商品后台顺序、名称和 slug 确保结果稳定；
- 报价状态筛选为 `all / available / unavailable`。

核心商品层级只使用明确名称规则：ChatGPT Plus/Pro/Team、Claude Pro/Max/Team、Gemini Pro/Ultra、Grok/SuperGrok、Cursor。它是编辑优先级，不虚构点击或销量。

### 2.2 修复实时导入的排序方向

`import-priceai-snapshot.ts` 的源序号改为随 index 递增，保持“源站越靠前，目录值越小”的升序约定。

抽出可测试的 `catalogSortOrderForSourceIndex()`，阻止未来再次把方向写反。

### 2.3 页面控制

`CardProductsClient.tsx` 增加：

- 数据状态摘要：标准商品、有在售、暂无在售；
- 推荐规则说明；
- 排序选择：推荐、渠道最多、价格最低、最近更新；
- 报价状态：全部、有在售、暂无在售；
- 排序和状态写入 URL，便于分享和返回；
- 平台、搜索、状态或排序变化时重置可见数量；
- 初始显示 24 条，之后每次加载 24 条；
- 保留“全部报价”入口和完整标准商品数量。

### 2.4 目录表重排

`MasterTable.tsx` 改为一张跨平台可比较的规格表，不再让平台分组阻断全局顺序。

桌面行：

- 当前序号；
- 商品名、平台、目录 ID、短说明；
- 最低价；
- 在售渠道；
- 最近更新；
- 查看详情。

手机卡片保留同样的决策信息。无渠道显示“暂无在售”，无更新时间显示“待首次采集”。

### 2.5 回归门禁

新增单元测试：

- 无在售商品永远排在有在售商品后；
- ChatGPT Plus 主市场条目在推荐序列最前；
- 同名商品按渠道覆盖优先；
- 渠道、价格、更新时间排序正确；
- 筛选不会删除源数组；
- PriceAI 源 index 与目录升序一致。

增强视觉审计：

- `/card-products` 首行有在售；
- 前 20 行没有暂无在售；
- 首三行包含 ChatGPT Plus；
- 320/390/768/1280px 无溢出、坏图、按钮换行和控制台错误；
- 日夜主题均通过。

## 3. P1：同名目录治理（独立数据库发布）

本轮只改善辨认和排序，不直接删除/停用 16 组同名目录。后续数据库发布按以下顺序执行：

1. 新增 `canonical_catalog_id` 或单独的 alias 表；
2. 为 16 组生成候选映射；
3. 对比 short_desc、搜索词、报价规格和历史 URL，人工/规则确认；
4. 把旧 slug 保留为 301/308 或详情 alias；
5. 将报价迁移到 canonical 商品，验证数量守恒；
6. 只有在 URL、报价和 SEO 都验证后，才把重复目录从主列表隐藏；
7. 回滚时恢复 alias 指向，不丢报价。

完成标准：标准商品列表不再有无法区分的同名行，但所有旧链接继续可访问。

## 4. P1：详情与卖家决策

后续增加：

- 最低价、中位数、P25/P75、有效样本数；
- 7/24/72 小时新鲜度；
- 有货/缺货数量；
- 明确质保标签的质保最低价；
- 异常低价提示，不自动判定诈骗；
- 来源健康和最后成功；
- 2–4 个报价本地对比；
- 预填利润计算器与账号商机上下文。

这些指标必须来自真实报价，不用模型补齐。

## 5. P2：真实热度和收益归因

推荐排序上线后再建立匿名、低数据量事件：

- 搜索词命中；
- 商品详情点击；
- 原站链接点击；
- 利润计算完成；
- 商机 → 商品 → 利润工具路径；
- 收藏/提醒（用户明确选择后）。

达到足够样本前继续使用透明规则排序。达到样本门槛后，可增加“近期关注”但必须显示统计窗口和最小样本，不能把广告位混入自然排序。

## 6. 其他模块路线

| 阶段 | 模块 | 工作 |
|---|---|---|
| P1 | 全部报价 | 价格、库存、更新时间排序；URL 状态；结果总数 |
| P1 | 渠道 | 覆盖商品、有效报价、新鲜度、错误率和健康排序 |
| P1 | 官方价格 | 等独立日程验证 Credits 修复，不与本轮部署耦合 |
| P1 | 账号商机 | 增加历史分页、商品标签、执行状态和停止条件 |
| P2 | 社区 | 真实账户、帖子、回复、证据、更正和审核流 |
| P2 | 指南 | 质保、异常低价、库存、来源健康和利润操作内容 |
| P2 | 商业化 | 可验证展示位、点击归因、批量需求与源头供给线索 |

## 7. 修改文件

本轮预计：

- `SUPPLY_RADAR_PRODUCT_DISCOVERY_GAP_ANALYSIS.md`
- `SUPPLY_RADAR_PRODUCT_DISCOVERY_OPTIMIZATION_PLAN.md`
- `v2-web/src/lib/product-ranking.ts`
- `v2-web/src/lib/product-ranking.test.ts`
- `v2-web/src/lib/priceai-import.ts`
- `v2-web/src/lib/priceai-import.test.ts`
- `v2-web/scripts/import-priceai-snapshot.ts`
- `v2-web/src/app/card-products/CardProductsClient.tsx`
- `v2-web/src/components/MasterTable.tsx`
- `v2-web/scripts/visual-audit.ts`
- `scripts/release-ui-audit.ts`

如实现中没有必要，不修改数据库函数、详情路由、官方价格、AI 日报或 Worker 后端。

## 8. 测试与发布

按顺序执行：

1. `pnpm test`（V2 Node 测试）；
2. `pnpm typecheck`；
3. `pnpm lint`（继承 warning 单列，error 必须为 0）；
4. `pnpm audit:data` 的链接 Supabase 只读检查；
5. `pnpm build`；
6. 本地/预览 Worker 的视觉审计；
7. Root lint、typecheck、test、content check、seed/opportunity dry-run 和 build；
8. 提交任务分支并 SSH 推送；
9. 等待源码仓库 CI；
10. 预览 Worker 部署并验证；
11. 生产部署最多一次；
12. 验证生产状态码、首 20 行、ChatGPT Plus 位置、筛选、URL、canonical、移动端和日夜主题。

## 9. 稳定边界

- 不删除 71 个标准商品中的任何一项；
- 不删除 5,363 条报价或历史链接；
- 不把渠道数叫作销量；
- 不用缺货或无报价条目凑“热门”；
- 不修改、触发或阻塞 AI 日报；
- 官方价格同步、社区和账号商机失败不影响商品主体；
- 不输出 Supabase、GitHub 或 Cloudflare 凭据；
- 测试或视觉门禁失败时不部署生产。

## 10. 回滚

- 代码回滚：对本轮提交执行 `git revert <本轮提交 SHA>`；
- 部署回滚：使用已有 `rollback-supply-v2.yml` 回到上一个生产 SHA；
- 本轮不做生产数据迁移，因此代码回滚不会造成报价或目录数据丢失；
- 若实时导入排序方向修改影响后台手工顺序，可先 revert 导入 helper，推荐排序仍可独立工作。

## 11. 完成标准

- 文档、代码和回归测试均在任务分支；
- 默认商品页先服务核心买家任务；
- 无报价商品仍完整可见但不再阻塞首屏；
- 用户可以自己选择排序和状态；
- 同名目录风险被清楚记录且没有粗暴删除；
- CI、构建、预览、生产与线上验收全部通过；
- Cloudflare 与 AI 调用变化有实际报告。

## 12. 本地执行记录（2026-08-30）

- 只读 Supabase 审计通过：13 个平台、71 个标准商品、380 个渠道目标、5,363 条报价；54 个商品有在售报价，17 个暂无在售；无无效 HTTPS、孤立商品或孤立渠道；
- 推荐排序回归通过：首项为 ChatGPT Plus，前 24 项全部有在售报价；完整 71 项可经“加载更多”到达，暂无在售从第 55 项开始；
- V2 Node 测试 33/33、TypeScript、Next.js 生产构建通过；ESLint 0 error，101 个仓库既有 warning，本轮未新增；
- Root Worker 测试 19/19、TypeScript、内容校验、seed dry-run、账号商机 dry-run 与 Wrangler dry-run 通过；
- 商品页发布级审计 320/390/768/1280px 共 4/4 通过；商品页桌面/手机、日间/夜间共 4/4 通过，均无溢出、坏图或控制台错误；
- Hallmark 自检：P5 H5 E4 S5 R5 V4；本轮沿用既有 Workbench 设计系统，没有新主题、无装饰性动画、无虚构热度或销量指标，适用的 58 项 slop gate 无阻断项；
- 本轮只读取生产报价，没有写库、迁移或调用 AI；预览与生产部署结果在发布完成后记录到最终报告。
