# 爱窝啦 AI 货源雷达：OpenPrice 商业授权版 V2 迁移实施方案

> 状态：执行中
>
> 制定日期：2026-08-29（Asia/Shanghai）
>
> 生产基线：`Aivora-Supply-Radar@37c275116d47d6498d9b4c4b0e272e5df4975cb7`
>
> 授权源码基线：`awesome-OpenPrice@387d6b2b5a7ab0a42acc42da2117c9fd0cf290bf`
>
> 正式域名：<https://supply.aivora.cn/>
>
> 品牌：爱窝啦·AI账号店
>
> 主站：<https://www.aivora.cn/>

## 1. 项目目标

在不影响 AI 日报主体和当前货源站生产版本的前提下，使用已取得商业授权的 OpenPrice 源码建设 V2：

- 完整移植 OpenPrice 的标准商品、全网报价、渠道、官方地区价格、指南、投稿反馈和管理后台；
- 保留并增强爱窝啦已有的报价快照、价格异动、账号商机、利润试算、社区和投稿能力；
- 建立可扩展的实时采集引擎，形成 100 家以上商家、10,000 条以上有效报价的首期数据规模；
- 将“行情信号 → 找货源 → 算利润 → 上架销售 → 记录结果”做成闭环；
- 保留当前 Worker 和正式站点作为切流前基线及切流后回滚入口。

V2 的完成标准不是“页面存在”，而是数据规模、新鲜度、功能闭环、测试、部署和回滚全部达到本方案规定的验收线。

## 2. 已确认事实与前提

### 2.1 商业授权

- 项目所有者已明确确认取得 OpenPrice 代码商业授权，并允许移植全部模块。
- 实施中保存授权版本、授权范围和交付清单，但不把包含个人信息、金额或敏感条款的授权文件提交到公开仓库。
- 若授权不包含公开再分发，授权源码只进入私有仓库或私有依赖；这不减少线上功能。
- 不把 OpenPrice 的品牌、域名、推广素材和第三方广告误当作爱窝啦资产；所有公开页面统一使用爱窝啦品牌。

### 2.2 当前爱窝啦生产基线

- 48 个标准商品；
- 24 条爱窝啦自营报价；
- 1 家商户；
- 17 个商品有报价，31 个商品为空目录；
- 2 期账号商机；
- 当前技术栈为 Hono Worker + 单个 SQLite Durable Object + Pages service binding；
- 当前生产 Worker、域名、数据库和 Pages edge 在 V2 切流前不得覆盖或删除。

### 2.3 OpenPrice 授权源码基线

当前已核对的源码版本为 `387d6b2b5a7ab0a42acc42da2117c9fd0cf290bf`，包含：

- Next.js/OpenNext Cloudflare 前台；
- 标准商品、全部报价和商品详情；
- 渠道目录和渠道详情；
- App Store 官方地区价格；
- 指南和博客；
- 后台登录、概览、平台、类目、采集目标、报价、投稿和反馈管理；
- Supabase PostgreSQL schema 和 RLS；
- App Store Python 价格采集任务；
- R2 ISR、Durable Object cache queue 和 tag cache。

当前公开源码未包含完整的多发卡系统采集引擎。若授权方另行交付私有采集代码，先审计后接入；若没有，则按本方案自行补齐适配器。

## 3. 稳定边界

1. `Aivora-Supply-Radar` 始终独立于 AI 日报生产链路，只读取已发布的账号商机 Markdown，不触发日报生成或 repair。
2. 单一货源、图片、社区、曝光、商机或官方价格失败，不得使核心商品目录和上次成功报价不可用。
3. AI 日报、AI 商机和 AI 账号商机的原仓库、Worker、Cron 和部署流程不得被 V2 修改。
4. 当前生产站在 V2 完成数据对账、测试和灰度验证前保持不变。
5. 原始商家名称、商品标题、来源 URL、价格、库存、采集时间和状态必须保留。
6. 不用虚假、过期或无法回到原站核验的内容填充报价数量。
7. 管理密钥、Supabase service role、Cloudflare Token 和授权文件不得出现在日志、提交或前端包中。

## 4. 目标技术架构

```text
用户
  │
  ▼
Cloudflare OpenNext Worker（V2 Web）
  ├─ 商品、报价、商家、异动、官方价、商机、社区、避坑
  ├─ 管理后台
  ├─ R2 ISR cache
  └─ Cache API 页面缓存
  │
  ▼
Supabase PostgreSQL
  ├─ 市场核心表
  ├─ 报价快照和异动
  ├─ 商机、社区、曝光、收藏和提醒
  ├─ 聚合统计读模型
  └─ RLS / 管理权限

Cloudflare Cron
  │
  ▼
Collector Worker → Cloudflare Queues → 单来源适配器
  │                                  ├─ 链动小铺
  │                                  ├─ 独角数卡
  │                                  ├─ 二次元发卡
  │                                  ├─ JSON Feed / JSON-LD
  │                                  ├─ 官方价格
  │                                  └─ 授权版其他适配器
  ▼
R2 原始快照和证据 → 规范化/分类/去重/校验 → PostgreSQL
```

### 4.1 为什么使用 OpenNext + Supabase

- 授权代码的前台、后台、查询和 RLS 已经围绕 Next.js 与 Supabase 实现，保留技术栈才能真正复用全部模块；
- 强行迁入现有 Hono 页面会把“移植”变成大规模重写；
- PostgreSQL 更适合数万报价、后台查询、关联统计和服务端分页；
- Cloudflare 继续负责部署、缓存、定时调度、队列、原始快照和正式域名；
- 当前 Hono Worker 保留为回滚服务，不作为 V2 大规模行情数据库。

## 5. 模块迁移矩阵

| 模块 | 来源 | 动作 | V2 验收重点 |
|---|---|---|---|
| 商品目录 | OpenPrice | 直接移植、品牌化、URL 兼容 | 聚合数据准确、可筛选 |
| 全部原始报价 | OpenPrice | 移植后改服务端分页 | 30,000 条不返回全量 JSON |
| 商品详情 | OpenPrice + 爱窝啦 | 合并报价、历史、利润、商机 | 真实来源、快照、交叉链接 |
| 商家与渠道 | OpenPrice | `/channels` 迁到 `/merchants` | 来源健康、商品覆盖、更新时间 |
| 官方地区价格 | OpenPrice | 整体移植 | 地区、币种、套餐、最后核验 |
| 商品/平台/报价后台 | OpenPrice | 整体移植并补测试 | 权限、分页、审计 |
| 采集目标和投稿审核 | OpenPrice | 整体移植并扩展调度字段 | 审核后真正进入采集队列 |
| App Store 采集 | OpenPrice | 移植 Python 任务 | dry-run、汇率和写入验证 |
| 报价快照/异动 | 爱窝啦 | 迁入 PostgreSQL 并增强 | 首次为基线，真实变化才入榜 |
| AI 账号商机 | 爱窝啦日报 | 只读同步并结构化 | 不触发日报、关联货源和利润 |
| 社区 | 爱窝啦 + 新实现 | 增加分类、回复、审核 | 货源、求购、优惠、问答 |
| 避坑曝光 | 新实现 | 建立证据、反证、更正流程 | 可按商家和标识检索 |
| 收藏/提醒 | 新实现 | watchlist + alert jobs | 降价、补货、下架提醒 |
| 卖家经营工具 | 新实现 | 价差、利润、缺货和需求信号 | 能产生可执行动作 |

## 6. 数据模型

### 6.1 保留 OpenPrice 核心表

- `product_platforms`
- `product_catalog`
- `crawler_targets`
- `market_offers`
- `user_target_submissions`
- `user_feedbacks`
- `apple_store_apps`
- `apple_store_prices`

### 6.2 V2 扩展表

- `offer_snapshots`：价格、库存和状态快照；
- `price_events`：涨价、降价、补货、缺货、上架、下架；
- `source_runs`：每次采集的发现数、接受数、拒绝数、耗时和错误；
- `source_schedules`：更新级别、下次运行时间、退避和熔断；
- `merchant_profiles`：商家公开资料和状态；
- `merchant_metrics`：成功率、新鲜度、商品数和价格竞争力；
- `raw_documents`：R2 object key、哈希、HTTP 元数据和解析版本；
- `opportunities`、`opportunity_products`：账号商机及其关联商品；
- `community_posts`、`community_replies`：社区内容；
- `exposure_cases`、`exposure_evidence`、`exposure_responses`：曝光证据链；
- `watchlists`、`alert_rules`、`alert_events`：收藏和提醒；
- `product_stats`、`merchant_stats`：面向公开页面的聚合读模型；
- `outbound_clicks`：不含身份隐私的渠道点击统计。

### 6.3 唯一键与去重

- 报价唯一键优先使用 `target_id + source_offer_id`；
- 没有稳定 ID 时使用规范化商品 URL；
- 最后才使用商家、规范化标题、规格和交付方式指纹；
- 不合并不同商家的同名商品；
- 套餐区间不能把最低规格价格冒充全部规格价格；
- `unknown` 库存不能当作 `in_stock`。

## 7. 采集设计

### 7.1 调度级别

| 级别 | 适用来源 | 目标频率 |
|---|---|---|
| Hot | 热门商品、成交活跃渠道 | 5-10 分钟 |
| Active | 正常活跃商家 | 30-60 分钟 |
| Normal | 更新较少的商家 | 3-6 小时 |
| Cold | 长尾或长期无变化来源 | 12-24 小时 |

### 7.2 单来源任务边界

- HTTPS only；
- URL、重定向和私网地址保护；
- 连接、响应和总任务超时；
- 响应类型和最大字节数限制；
- 有界分页和最大商品数；
- 原始响应哈希和 R2 快照；
- 解析失败保留 last-good 数据；
- 重试采用退避，不在一次 Cron 内循环所有来源；
- 相同来源不得并发执行两个采集任务。

### 7.3 数据质量门槛

- 原始来源 URL 可访问；
- 价格为合理数字，币种可识别；
- 商品标题非空且未被广告文本污染；
- 库存和质保只使用来源明确给出的信息；
- 分类规则为“明确类目 ID > 商家映射 > 规则匹配 > 人工审核”；
- AI 可辅助生成候选分类，但不得在高频采集热路径直接决定公开类目；
- 异常低价、价格为零、标题过短、来源跳转异常进入隔离队列。

## 8. 网站信息架构

```text
爱窝啦 AI 货源雷达 (/)
├── 行情中心 (/products)
│   └── 标准商品 (/products/{slug})
├── 全网报价 (/offers)
├── 商家与渠道 (/merchants)
│   └── 商家详情 (/merchants/{slug})
├── 今日异动 (/changes)
├── 官方价格 (/official-prices)
│   └── 应用地区价格 (/official-prices/{app})
├── AI 账号商机 (/opportunities)
│   └── 每日商机 (/opportunities/{date})
├── 社区与避坑 (/community)
│   ├── 货源发布 (/community/supply)
│   ├── 求购需求 (/community/wanted)
│   ├── 羊毛优惠 (/community/deals)
│   ├── 使用问答 (/community/qa)
│   └── 避坑曝光 (/risk)
├── 指南 (/guides)
├── 提交渠道 (/submit)
└── 管理后台 (/admin，不进入公开导航和 sitemap)
```

### 8.1 主导航

1. 行情
2. 全网报价
3. 商家
4. 异动
5. 商机
6. 社区与避坑
7. 提交渠道（CTA）

### 8.2 URL 兼容

| OpenPrice 原 URL | V2 URL | 处理 |
|---|---|---|
| `/card-products` | `/products` | 301 |
| `/card-products/all` | `/offers` | 301 |
| `/card-products/{slug}` | `/products/{slug}` | 301 |
| `/channels` | `/merchants` | 301 |
| `/channels/{id}` | `/merchants/{slug}` | 映射后 301 |
| `/guide` | `/guides` | 301 |

爱窝啦当前已经公开的 `/products`、`/merchants`、`/changes`、`/opportunities`、`/community`、`/submit`、`/methodology` 保持有效。

### 8.3 内部链接闭环

- 商品 → 报价 → 商家 → 反馈/曝光；
- 商品 → 价格历史 → 今日异动；
- 商机 → 关联商品 → 可用货源 → 利润试算；
- 社区求购 → 相关商品与商家；
- 商家 → 全部在售 → 历史上下架 → 来源健康；
- 指南 → 对应商品、官方价和风险页；
- 每个公开详情页至少有一个可回到栏目页的面包屑和两个上下文链接。

## 9. 性能改造

OpenPrice 当前存在全量报价读取路径，不能在导入大数据前直接上线。V2 必须先完成：

- `/offers` 使用游标分页，每页 50-100 条；
- 搜索、品类、平台、商家、价格、库存、质保、交付和更新时间全部服务端筛选；
- `product_stats` 提供最低价、质保最低价、有货数、渠道数和最后更新时间；
- `merchant_stats` 提供报价数、商品数、成功率和新鲜度；
- 首页不扫描全量 `market_offers`；
- 后台报价和来源列表分页；
- 公开 API 响应设置条数上限和速率限制；
- 热门公开页使用 R2 ISR/Cache API，查询参数页面不写入公共 HTML cache；
- 数据变化按商品、商家和列表 tag 精确失效缓存。

性能验收目标：

- 30,000 条报价下，不存在一次返回全量报价的公开接口；
- 缓存命中页面目标响应时间小于 1.5 秒；
- 冷查询目标响应时间小于 3 秒；
- 单个列表 API 正常响应体目标小于 500 KB；
- 390px 手机视口筛选和分页不卡死、无横向页面溢出。

## 10. 商机和赚钱闭环

账号商机不只展示 Markdown，需要结构化为：

- 信号类型和证据；
- 关联标准商品；
- 当前最低货源和可用渠道数；
- 建议零售价；
- 平台费率和预计利润；
- 买家类型；
- 是否适合今天上架；
- 可信度和售后风险；
- 观察期限、继续条件和停止条件；
- 执行状态：待评估、已上架、已忽略、已验证。

收入路径：

- 爱窝啦自营商品成交；
- 商家认证资料页；
- 清晰标记的推广位；
- 专业版价格和补货提醒；
- 卖家价差、缺货和需求监控；
- CSV/API 数据服务；
- 渠道线索和合作服务。

爱窝啦自营报价可以显示“自营、已核验”，但自然报价表按公开排序规则运行，推广位必须明确标记。

## 11. 分阶段执行计划

### Phase 0：授权资产与基线固化

- [x] 从最新 `origin/main` 创建独立 worktree 和任务分支；
- [x] 核对 OpenPrice 本地参考副本与远端 main SHA 一致；
- [x] 保存商业授权版 V2 实施方案；
- [x] 建立授权资产清单和来源版本记录；
- [ ] 明确完整采集器、渠道配置和数据库数据是否随授权交付；
- [x] 记录当前线上 health、商品、商家、异动、商机基线；
- [x] 记录当前生产 Worker、Pages edge 和回滚 service binding；
- [x] 为现有公开 URL 建立 V2 兼容测试清单。

验收：任何后续阶段都能回到当前 `37c2751`，授权源码版本可复现且资产范围没有未知项。

### Phase 1：授权版 V2 基线

- [ ] 在独立目录导入 OpenPrice 授权代码；
- [ ] 移除上游品牌和第三方推广素材；
- [ ] 统一品牌、正式域名、主站链接、主题和 SEO；
- [ ] 建立 `/products`、`/offers`、`/merchants` 路由和旧 URL 重定向；
- [ ] 保留完整后台和投稿反馈；
- [ ] 补齐前端单元/集成测试基线；
- [ ] 使用 OpenNext dry-run 构建；
- [ ] 部署到独立预览 Worker，不切正式域名。

验收：授权模块完整可访问，生产站不变，预览环境可随时删除或重建。

### Phase 2：数据库与数据迁移

- [ ] 创建生产/预览隔离的 Supabase 项目和密钥；
- [ ] 应用 OpenPrice schema 和迁移；
- [ ] 添加 V2 扩展表、索引和 RLS；
- [ ] 导入 48 个现有标准商品和 slug 映射；
- [ ] 导入 24 条爱窝啦报价及快照；
- [ ] 导入现有官方价格和账号商机；
- [ ] 对账价格、币种、URL、图片、日期、库存和关联；
- [ ] 生成可重复、幂等的数据迁移命令。

验收：迁移重复运行不产生重复数据，关键字段逐项对账通过。

### Phase 3：大数据量与查询改造

- [ ] 删除或封闭全量报价公开响应；
- [ ] 实现服务端游标分页和全套筛选；
- [ ] 建立商品与商家聚合读模型；
- [ ] 后台列表分页；
- [ ] 建立 10,000 和 30,000 条报价性能 fixture；
- [ ] 完成缓存 tag 和精确失效；
- [ ] 完成 API 限流和响应上限。

验收：达到第 9 节全部性能目标。

### Phase 4：全网采集

- [ ] 审计并接入授权版完整采集器；
- [ ] 接入链动小铺、独角数卡、二次元、JSON Feed、JSON-LD；
- [ ] 建立 Cron、Queue、R2 原始快照和来源锁；
- [ ] 建立重试、退避、熔断和 last-good；
- [ ] 建立分类、去重、异常隔离和人工审核；
- [ ] 建立来源健康和采集运行后台；
- [ ] 先接 10 家验证，再扩展到 30 家、100 家；
- [ ] 达到 10,000 条有效报价。

验收：100 家以上活跃商家、10,000 条以上有效报价，任一来源失败不影响主体。

### Phase 5：爱窝啦差异化模块

- [ ] 价格快照和异动；
- [ ] 利润试算；
- [ ] 账号商机只读同步和结构化；
- [ ] 商机与实时货源关联；
- [ ] 社区分类、帖子、回复、审核；
- [ ] 曝光、证据、反证和更正；
- [ ] 收藏、降价和补货提醒；
- [ ] 卖家经营面板；
- [ ] 爱窝啦自营身份和公开排序边界。

验收：买家能完成比价和风险判断；卖家能从商机进入货源并算出利润。

### Phase 6：完整验证

- [ ] lint；
- [ ] TypeScript typecheck；
- [ ] 前端单元与集成测试；
- [ ] Python 采集器测试；
- [ ] 采集 fixture 和失败隔离测试；
- [ ] 数据迁移对账；
- [ ] RLS、后台权限、会话和限流测试；
- [ ] 10,000/30,000 数据性能测试；
- [ ] canonical、Schema、robots、sitemap；
- [ ] 桌面与约 390px 手机；
- [ ] 日间、夜间和系统主题；
- [ ] 图片、外链、分页和重定向；
- [ ] AI 日报独立性回归；
- [ ] OpenNext production build；
- [ ] 预览 Worker smoke test。

### Phase 7：灰度发布与切流

- [ ] 部署 V2 独立 Worker；
- [ ] 验证预览域名；
- [ ] 冻结短时间写入并执行最终增量迁移；
- [ ] 切换 Pages service binding 到 V2；
- [ ] 验证正式域名所有核心 URL；
- [ ] 验证缓存、移动端、SEO 和后台；
- [ ] 保留旧 Worker 和旧数据库作为回滚入口；
- [ ] 观察来源成功率、错误率和页面响应时间。

## 12. 测试与质量门槛

测试不通过不得切正式域名。至少包括：

1. 商品分类规则的包含、排除、类目 ID 和人工覆盖测试；
2. 每个采集适配器的固定 HTML/JSON fixture；
3. URL、重定向、私网、超时、最大响应和 Content-Type 安全测试；
4. 首次快照不产生虚假涨跌；
5. 价格、库存、质保和商品上下架变化测试；
6. 迁移幂等和数量对账；
7. 公开 anon key 与后台 service role 权限边界；
8. 管理后台登录、限流、CSRF/Origin 和会话失效；
9. 列表分页、排序、筛选和缓存失效；
10. 关键页面 SEO 与结构化数据；
11. 30,000 条报价下的页面和 API 性能；
12. 商机同步失败时商品和报价仍正常；
13. 单个来源失败时其他来源继续采集；
14. V2 故障时旧生产 Worker 可立即恢复。

## 13. 部署策略

发布顺序：

1. 本地测试和 OpenNext production build；
2. 独立 V2 Worker 的 `workers.dev` 预览；
3. 独立预览域名或内部 service binding；
4. 数据对账和最终验收；
5. Pages edge 切换到 V2 Worker；
6. 正式域名验证；
7. 观察窗口结束后再决定旧数据归档，旧 Worker 长期保留为低成本回滚入口。

正式生产任务最多触发一次。导入和采集首次运行前必须先 dry-run，并检查发现数、接受数、拒绝数、预计写入数和资源消耗。

## 14. 回滚方案

### 14.1 代码回滚

- 将 Pages service binding 从 V2 Worker 恢复到现有 `aivora-supply-radar`；
- 或重新部署切流前提交；
- 不需要删除 V2 Worker、Supabase 或 R2 数据即可恢复公开站点。

### 14.2 数据回滚

- 数据迁移仅复制，不删除旧 Durable Object 数据；
- 每次 schema migration 提供对应 down/forward-fix 说明；
- 采集错误优先禁用单一 target，而不是回滚全站；
- 错误分类通过人工覆盖和重新生成聚合数据修复；
- 原始 R2 快照用于重新解析，避免重复访问来源站。

### 14.3 内容回滚

- 商机、社区和曝光独立开关；
- 任一可选模块可隐藏，但商品和已核验报价继续服务；
- 不回滚或修改 AI 日报生产内容。

## 15. 量化验收指标

| 指标 | V2 首期目标 |
|---|---:|
| 活跃商家 | ≥ 100 |
| 有效报价 | ≥ 10,000 |
| 标准商品 | ≥ 60，按真实市场逐步扩展 |
| 热门商品渠道数 | ≥ 20 |
| 热门来源新鲜度 | 5-10 分钟目标 |
| 活跃报价按期更新率 | ≥ 80% |
| 来源任务成功率 | ≥ 95% |
| 抽检分类准确率 | ≥ 95% |
| 缓存命中页面目标响应 | < 1.5 秒 |
| 冷查询目标响应 | < 3 秒 |
| 公开列表 API 单页 | ≤ 100 条 |
| 单一来源故障影响 | 只影响该来源 |
| AI 日报生产依赖 | 0 |

## 16. Phase 0 授权资产清单

| 资产 | 当前状态 | 后续动作 |
|---|---|---|
| 商业授权确认 | 项目所有者已确认 | 记录授权版本和私密证据保存位置 |
| OpenPrice public source | 已核对 `387d6b2` | 固定导入来源和提交 |
| 完整私有采集器 | 待核对 | 确认是否另行交付 |
| 发卡系统适配器 | 待核对 | 列出类型、fixture 和依赖 |
| 渠道目标配置 | 待核对 | 确认是否可随授权使用 |
| 现有生产数据库导出 | 待核对 | 若无则从原商家重新采集 |
| 商品类目种子 | public schema/线上可见 | 授权版导入并与爱窝啦合并 |
| 品牌素材 | 不继承 OpenPrice 品牌 | 统一换为爱窝啦素材 |
| 部署密钥 | 不继承 | 在爱窝啦环境重新创建 secrets |

## 17. 当前执行状态

- 已创建独立 worktree：`D:\GitHub\_worktrees\authorized-openprice-v2`；
- 已创建任务分支：`codex/authorized-openprice-v2`；
- 基线来自 `origin/main@37c2751`；
- 当前生产站未修改；
- 已新增 `AUTHORIZED_ASSET_INVENTORY.md` 和 `V2_PRODUCTION_BASELINE.md`；
- 下一步：验证规划文档、提交 Phase 0 已完成部分，然后开始授权源码基线导入与构建。
