# 爱窝啦·货源雷达库存与买卖体验检查报告

日期：2026-08-30（Asia/Shanghai）

检查对象：`https://supply.aivora.cn`、生产 Supabase 数据、GitHub Actions 数据同步、PriceAI、AIDeal、OpenPrice、AI 原住民之家

事实基线：线上页面、`origin/main`、生产接口与最近一次工作流日志；不以旧本地 HEAD 作为结论依据。

## 1. 本轮目标与验收口径

本轮首先解决“买家无法一眼判断能否购买、缺货商品没有可靠沉底”的问题，同时检查卖家选品是否获得足够的竞争信息。验收不以“页面能打开”为准，而以以下业务结果为准：

1. 商品库存状态以商品本身的公开库存信号为准，不能把来源网站可访问误当成商品有货。
2. 可购买报价必须排在缺货、下架报价之前；缺货报价保留但视觉降级，购买按钮禁用。
3. `在售/可购买` 数字、列表状态、库存数字和实际可点击操作必须一致。
4. 买家可切换“全部、可购买、缺货/下架”，并能看到各状态数量。
5. 卖家仍可查看缺货市场、价格和更新时间，用于判断供给缺口，但不会干扰买家下单。
6. 半小时同步面对活跃数据源的正常增减不能轻易整批失败，同时仍要保留完整性阈值。
7. 不减少现有 71 个标准商品和 5,000+ 条真实报价，不用虚构库存或垃圾数据凑数。

## 2. 线上量化证据

### 2.1 商品目录不是唯一问题

生产 `/card-products` 首屏当前显示 24 个商品，前三项为：

1. ChatGPT Plus 试用订阅
2. Gemini Pro 成品号
3. ChatGPT Pro 20x

首屏不存在“暂无在售商品插在有货商品前面”的情况，说明上一轮目录推荐排序已经生效。当前核心问题转移到了报价库存语义、详情页表达和同步新鲜度。

### 2.2 ChatGPT Plus 的生产状态严重失真

检查生产接口 `/api/products/chatgpt-plus/offers` 并完整翻页后得到：

| 指标 | 生产结果 |
| --- | ---: |
| 总报价 | 1,082 |
| 被标记为 `in_stock` | 971 |
| 被标记为 `out_of_stock` | 111 |
| `in_stock` 但 `inventory=0` | 623 |
| 缺货后又出现有货 | 0 |

排序本身按照数据库枚举 `in_stock → out_of_stock → offline → blacklisted` 执行，971 条“有货”确实排在前面；但其中 623 条又显示“库存: 0”。因此页面同时告诉买家“正常”“库存 0”“前往购买”，形成直接冲突。

### 2.3 上游状态字段被错误解释

PriceAI 当前公开接口同时包含：

- `status`：商品库存，例如 `in_stock`、`low_stock`、`out_of_stock`；
- `effectiveStatus`：来源/记录是否仍可用，例如 `available`、`unavailable`。

现有 `normalizePriceAiStatus()` 使用了“`status` 是 in_stock **或** `effectiveStatus` 是 available 就算有货”的逻辑。这会把 `out_of_stock + available` 错写为 `in_stock`。

对 PriceAI 当前 ChatGPT Plus 1,093 条公开记录抽取全量状态，看到大量 `out_of_stock + available`。PriceAI 页面本身只把真正的商品有货记录计入“有货”，而本项目把来源仍在线的缺货记录也计入 971 个“在售渠道”。这解释了生产中 623 条“有货但库存 0”。

正确业务优先级应为：

1. `hidden=true` → 黑名单；
2. 原始商品 `status=in_stock/low_stock` → 可购买；
3. 原始商品 `status=out_of_stock` → 缺货；
4. 原始商品 `status=offline` → 下架；
5. 未知值 → 下架/未知，绝不能因为来源可访问而晋升为有货。

### 2.4 实时同步已经中断

最近一次 `Supply Radar V2 Data Sync` 工作流运行 `33309967819` 失败，错误为：

`priceai_total_changed:chatgpt-pro-20x`

原因是导入器要求同一商品每一页返回的 `total` 完全相同。活跃货源在分页的几十秒内正常新增、下架或变更就会让整批同步失败。当前工作流计划每小时第 17、47 分运行，但最近一次失败后生产库存明显落后于上游。

必须保留严格性，但严格性应检查“分页漂移是否在合理阈值、唯一记录是否足够完整、URL/价格/关联是否有效”，不应把正常的实时增减等同于数据损坏。

### 2.5 修改前数据审计与修复后 dry-run 对照

生产只读审计（修改前）确认：71 个标准商品、380 个渠道目标、5,363 条报价；其中 4,962 条被标记为有货、401 条缺货，1,494 条出现“有货但库存 0”。数据关联、HTTPS URL 和唯一键检查均通过，说明问题集中在状态解释而不是数据伪造或关联损坏。

修复后的授权源全量 dry-run（零数据库写入）得到：44 个有效标准商品、5,489 条原始报价、5,378 条去重报价；3,399 条可购买、1,979 条缺货。57 条不合格记录被按既有严格规则跳过（36 条缺少身份、21 条不是有效 HTTPS URL），没有通过放宽校验凑数量。

## 3. 竞品库存与决策体验对照

### 3.1 PriceAI

当前 PriceAI 商品目录直接显示“有货、缺货、渠道”三个数字；ChatGPT Plus 详情明确写出“总报价、有货数量、按有货优先和低价排序”，并提供“库存 ≥50”“1 小时内更新”和商品特征标签。报价行将有货、库存量、渠道运营时长、更新时间、风险和购买入口放在同一决策路径。

本项目当前差距：

- 目录只显示一个被误算的“在售渠道”数字；
- 详情页没有状态筛选或状态统计；
- “正常 + 库存 0”互相冲突；
- 缺货行没有分区标题，价格仍是绿色；
- 没有“库存充足”“近期更新”等快捷筛选；
- 同名报价尚未折叠，信息密度高但比较效率低。

### 3.2 AIDeal

AIDeal 提供类别、细分、交付、价格、仅看有货、商户和更新时间等筛选，并把交易风险曝光作为购买前辅助信息。

本项目当前差距：

- 详情页只能搜索和按价格范围过滤；
- 买家不能一键仅看可购买；
- 卖家不能按交付方式、质保、库存或更新时间寻找供给缺口；
- 渠道反馈存在，但还没有形成可检索的风险层级。

### 3.3 OpenPrice 与 AI 原住民之家

OpenPrice 提供卡网商品、官方订阅、指南、渠道商等完整信息架构；AI 原住民之家通过持续社区帖子提供新货、批发、补货和实际需求信号。

本项目已有官方价、异动、账号商机、渠道商、指南和社区，模块广度并不低，但模块之间的转化链还弱：商品详情没有顺手跳到利润计算、异动和相关商机；社区供求信息也没有回流为可验证的结构化库存信号。

## 4. 买家体验问题清单

### P0：会直接阻碍下单

1. 库存语义错误，缺货被计入有货。
2. 有货状态与“库存 0”冲突。
3. 详情页缺少“全部/可购买/缺货下架”筛选和数量。
4. 缺货价格仍使用购买态绿色，弱化了不可购买提示。
5. “该商品有 X 个渠道报价”实际使用的是在售数，但文案像总数。
6. 半小时同步因正常数据漂移失败，导致状态不新鲜。

### P1：降低比价效率

1. 没有库存充足、近期更新、低风险、质保等快捷筛选。
2. 没有把重复/同名报价聚合为“最低价 + 展开 N 家”。
3. 没有把最低价异常、旧报价或极端离群价明确标记。
4. 详情列表很长，缺少状态分区和结果摘要。
5. 买家无法收藏商品、订阅补货或降价提醒。

### P2：建立长期信任

1. 渠道健康度目前主要是内部数据，前台缺少透明解释。
2. 风险反馈没有形成渠道级、证据级聚合。
3. 缺少一次购买后的“链接有效/库存准确/交付成功”轻反馈闭环。

## 5. 卖家体验问题清单

### P0/P1

1. 错误的在售数会夸大竞争强度，导致卖家放弃本来有缺口的商品。
2. 缺少“有货数/缺货数/总渠道数”的并列展示，无法看供需缺口。
3. 缺少近期新增、补货、断货、价格波动和低价持续时间等指标。
4. 利润计算器与商品详情未形成带入价格的快捷路径。
5. 账号商机日报没有在商品详情中按产品关联。
6. 没有渠道入驻后的覆盖率、价格位置、库存稳定性和点击反馈面板。

## 6. 技术根因

1. `src/lib/priceai-import.ts` 混淆商品 `status` 与来源 `effectiveStatus`。
2. `scripts/import-priceai-snapshot.ts` 把 `stockCount=0` 原样写入，同时错误状态又让 UI 显示“正常”。
3. 同一导入器把分页 `total` 的任何变化都视为致命错误，不适合实时源。
4. `ProductDetailClient` 没有 availability 状态；API 查询解析器也不支持库存筛选。
5. `DetailTable` 对 `inventory >= 0` 都显示库存，导致显示“库存 0”；缺货行未做足够视觉降级。
6. 现有视觉审计只验证状态码、溢出、图片和目录排序，没有验证库存文案、状态数量和不可购买分区。

## 7. 本轮修改边界

本轮实施 P0 和直接相关的 P1：修正导入语义、增强活跃分页容错、重跑一次生产同步、库存筛选/统计/分区、不可购买视觉降级、数据与视觉回归测试。收藏提醒、同名报价折叠、渠道信誉聚合和卖家后台列入后续迭代，不在本次修复中仓促上线。

本轮不修改 AI 日报 Worker，不触发 AI 日报生产任务，不减少商品和真实报价，不改变既有日夜主题与品牌视觉。

## 8. 验收数据

发布前后至少核对：

- 71 个标准商品不减少；
- PriceAI 真实报价保持 5,000+；
- 无无效 HTTPS URL、孤儿商品或孤儿渠道；
- `in_stock && inventory=0` 在重同步后应大幅下降，界面永不显示“可购买 + 库存 0”；
- ChatGPT Plus 可购买数应与上游同一时间窗口的原始 `in_stock + low_stock` 接近，而不是 971；
- 任一商品中都不得出现缺货报价排在可购买报价之前；
- 详情页“全部 = 可购买 + 缺货/下架”；
- 桌面与 390px 手机、日间与夜间无横向溢出、错色、失效按钮或字体跳变；
- 数据同步工作流完成并通过生产数据审计。
