# Cloudflare Worker configuration for OpenNext
name = "aivora-supply-radar-v2-preview"
main = "cloudflare-worker.mjs"
compatibility_date = "2026-08-28"
compatibility_flags = ["nodejs_compat"]
assets = { directory = ".open-next/assets", binding = "ASSETS" }
minify = true
keep_vars = true

# Cloudflare Images transforms Notion/R2 source images into WebP variants
# before the immutable results are uploaded to the public R2 bucket.
[images]
binding = "IMAGES"

# OpenNext ISR/data cache. Create once with:
# pnpm exec wrangler r2 bucket create aivora-supply-radar-v2-next-cache
[[r2_buckets]]
binding = "NEXT_INC_CACHE_R2_BUCKET"
bucket_name = "aivora-supply-radar-v2-next-cache"

# OpenNext uses a self-reference to perform background revalidation.
[[services]]
binding = "WORKER_SELF_REFERENCE"
service = "aivora-supply-radar-v2-preview"

[[durable_objects.bindings]]
name = "NEXT_CACHE_DO_QUEUE"
class_name = "DOQueueHandler"

[[durable_objects.bindings]]
name = "NEXT_TAG_CACHE_DO_SHARDED"
class_name = "DOShardedTagCache"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["DOQueueHandler", "DOShardedTagCache"]

# 提示: 本地开发与生产环境相关的秘钥及参数，如:
# NEXT_PUBLIC_SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, NOTION_API_KEY, CLOUDFLARE_R2_* 等
# 请在 Cloudflare Dashboard 对应的 Worker 详情页 -> Settings -> Variables and Secrets 中统一配置。
