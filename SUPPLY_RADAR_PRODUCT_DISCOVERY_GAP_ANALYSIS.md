# 爱窝啦·货源雷达商品发现与竞品差距分析

> 分析日期：2026-08-30（Asia/Shanghai）
> 代码基线：`232d3a72a064cdde89d871913735df094296d305`（`origin/main`）
> 生产基线：[https://supply.aivora.cn/card-products](https://supply.aivora.cn/card-products)
> 自动化证据：`artifacts/competitive-audit/report.json`（50/50 页面用例成功）
> 数据证据：生产 Supabase 只读审计、生产 DOM 顺序和公开竞品页面；没有使用或输出任何密钥。

## 1. 结论

当前问题不是“商品太少”，而是已有数据没有按照买家和卖家的决策顺序呈现。

- 生产已有 71 个标准商品、380 个来源、5,363 条报价，其中 4,962 条在售；规模已经可用；
- 只有 54 个标准商品存在在售渠道，17 个没有在售渠道；
- 默认首 20 行有 12 行显示“暂无报价 / 0 / 暂无更新”；
- ChatGPT 分类到第 32 行才出现，主要 ChatGPT Plus 聚合条目位于第 42 行，Claude 分类位于第 65–71 行；
- ChatGPT Plus 是当前最大商品，数据库有 1,082 条关联报价，公开摘要有 971 个在售渠道，却没有进入首屏；
- 目录存在 16 组同平台同名商品，共 32 行。多数是旧基线商品与 PriceAI 实时目录使用不同 slug 导致，不应简单删除，但必须让用户能辨认并逐步归并。

因此，本轮 P0 应是：**有在售渠道优先、核心 AI 商品优先、市场覆盖优先、更新时间兜底，并让用户能切换排序与报价状态。**

## 2. 当前线上事实

### 2.1 数据规模

| 指标 | 生产值 | 解释 |
|---|---:|---|
| 平台 | 13 | 标准目录平台，不等于来源数量 |
| 标准商品 | 71 | 全部保留，不因优化排序删减 |
| 有在售渠道的商品 | 54 | 可直接进入比价决策 |
| 暂无在售渠道的商品 | 17 | 应保留，但不应占据首屏 |
| 来源 | 380 | 采集目标/渠道来源 |
| 报价 | 5,363 | PriceAI 5,339，旧基线 24 |
| 在售报价 | 4,962 | 默认发现页的主要证据 |
| 缺货报价 | 401 | 详情页和异动判断仍需保留 |
| 最大商品 | ChatGPT Plus | 1,082 条关联报价，公开在售 971 |
| 无效 HTTP 商品链接 | 0 | 现有 HTTPS 边界有效 |
| 丢失商品/来源关联 | 0 / 0 | 数据引用完整 |

### 2.2 当前首屏顺序

生产 DOM 的前 20 行中：

- 第 1 行 `OpenAI API 额度`：暂无报价、0 渠道、暂无更新；
- 第 4–12 行中有 9 行暂无报价；
- 第 19–20 行的 Gmail 与“其他邮箱”也暂无报价；
- 合计 12/20 行没有在售渠道。

核心商品的位置：

| 商品/分类 | 当前位置 | 当前有效数据 |
|---|---:|---:|
| ChatGPT 分类首次出现 | 32 | — |
| ChatGPT Plus 试用订阅（旧基线） | 35 | 1 个在售渠道 |
| ChatGPT Plus 试用订阅（实时聚合） | 42 | 971 个在售渠道 |
| Cursor 账号 | 58 | 41 个在售渠道 |
| Gemini 分类 | 60–64 | 最大条目 363 个在售渠道 |
| Claude 分类 | 65–71 | 最大条目 119 个在售渠道 |

这直接违背“先解决最常见购买任务”的信息架构目标。

## 3. 根因

### 3.1 客户端覆盖了数据库的正确平台顺序

`get_product_catalog_summary()` 已按 `platform.sort_order → catalog.sort_order → name` 返回。

但 `CardProductsClient.tsx` 收到数据后重新执行：

1. 全局 `sort_order` 升序；
2. 平台中文名排序；
3. 商品名排序。

这一步丢弃了 `platform_sort_order`，使数据库和后台维护的平台优先级失效。

### 3.2 PriceAI 导入顺序方向与前端方向相反

`import-priceai-snapshot.ts` 把源列表第一个商品写为 `1000`，后续商品依次减小；客户端却按升序排列。因此源站越靠前的核心商品，在爱窝啦越靠后。

这是确定性的实现错误，不是数据波动。

### 3.3 平台分组放大了错误

`MasterTable` 先取错误排序中首次出现的平台，再把该平台所有商品整块展开。一个较早出现的长尾平台会挡住后续所有核心平台，用户无法跨平台看到真正优先的商品。

### 3.4 “0”表达的是缺数据，不是用户决策

无报价商品已经正确显示“暂无报价”，但“在售渠道”仍直接显示 `0`，“最近更新”显示“暂无更新”。连续的 `0` 给用户的感受是系统失效。

正确语义应为：

- `暂无在售`：当前没有在售渠道；
- `待首次采集`：没有成功更新时间；
- 商品仍保留在“暂无在售”筛选中，不伪造成低价或免费。

### 3.5 同名目录没有治理

生产有 16 组同平台同名商品，例如：

- `ChatGPT Plus 试用订阅`：`chatgpt-plus-trial` 与 `chatgpt-plus`；
- `ChatGPT Plus 正价代充`：`chatgpt-plus-renewal` 与 `chatgpt-plus-recharge`；
- `Claude Pro`：`claude-pro` 与 `claude-pro-month`；
- `Gmail / Google 邮箱`：`gmail-email` 与 `gmail-account`。

这些条目背后可能对应不同规格、来源或历史 URL。直接删除会破坏链接和报价归属；继续显示相同名称又会制造重复选择。正确后续方案是建立 canonical/alias 关系、保留旧 URL 重定向，并在数据迁移后合并报价，而不是删行。

## 4. 竞品对比

### 4.1 PriceAI

[PriceAI 卡网订阅](https://priceai.cc/channels) 当前公开：45 个标准商品、6,126 条报价、3,699 条有货、2,427 条缺货，最近更新约 5 分钟。

值得移植的能力：

- 默认从 ChatGPT 开始，ChatGPT Plus、Team/Business、Pro 在首屏；
- 首行同时展示最低价、质保最低价、有货/缺货数、渠道数、代表商家和更新时间；
- 明确说明“主价格优先取有货最低价”；
- 标准商品、全部报价、卡网商家三种任务可以直接切换；
- 平台、搜索和筛选入口始终可见。

不应照搬：广告密度、赞助内容和所有商品长页。

### 4.2 AIDeal / CloseMan

[AIDeal](https://closeman.asia/) 当前公开约 29,677 条报价，首批显示约 1,303 条；提供综合推荐、销量优先、价格最低/最高、库存最多、最近上新、自定义排序、仅看异动、仅看有货。

值得移植的能力：

- 排序不是后台内部值，而是用户能理解的任务语言；
- 热门商品行情独立展示参考价、最低价和日变化；
- “仅看有货”一键消除无效行；
- 库存、销量和地区/交付方式参与判断。

限制：爱窝啦目前没有真实用户点击、成交或销量数据，因此不能把“渠道数量”伪装成销量或真实热度。默认项应命名为“推荐”，并公开规则。

### 4.3 OpenPrice 授权源码

[OpenPrice 标准商品](https://www.openprice.cc/card-products) 保留平台分组和后台顺序，公开页面仍可看到 `¥0.00 / 0` 条目穿插在热门分类中。

这说明“完整复制授权源码”只能得到功能基线，不能自动得到更好的商品发现体验。爱窝啦必须在授权基线上增加自己的排序、状态语义和卖家工具。

### 4.4 WithAI

[AI 原住民之家](https://withai.homes/) 提供真实帖子、标签、回复、浏览量、最新/热门切换和加载更多。

值得移植的能力是供需互动和时效感，不是把未核验的帖子混入价格排序。社区内容必须与结构化商品、来源和证据分层。

## 5. 当前项目各模块差距

| 模块 | 当前状态 | 主要差距 | 优先级 |
|---|---|---|---|
| 标准商品 | 71 项，数据足 | 默认排序错误、无报价靠前、同名目录、无用户排序 | P0 |
| 全部报价 | 50 条一页，可加载更多 | 缺价格/更新时间/库存排序与可分享的排序 URL | P1 |
| 商品详情 | 1,000+ 报价可分页 | 缺价格分布、异常价、质保证据、来源健康摘要 | P1 |
| 渠道 | 380 个，已改为分页 | 缺覆盖商品数、健康度和最后成功的排序 | P1 |
| 异动 | 已有真实事件 | 缺商品详情和商机上下文入口 | P1 |
| 官方价格 | 页面仍为空 | 首次真实同步因新增 Credits 规格安全拒绝，待独立日程验证 | P1（独立链路） |
| 账号商机 | 目前仅少量日报 | 关联商品和利润入口已有，但历史、筛选和持续供给不足 | P1 |
| 社区 | 仍是三个入口卡 | 没有帖子、发布、回复、搜索和审核状态 | P2 |
| 指南 | 仅三篇基础指南 | 缺价格异常、质保、库存、来源健康和利润操作指南 | P2 |
| 收益闭环 | 利润计算器已上线 | 缺收藏、提醒、点击归因、线索状态和卖家工作台 | P2 |

## 6. 设计与交互审计

项目已有 `design.md`，本轮必须保持 Workbench、信号黄、证据蓝、系统中文字体、日夜主题与 4pt 间距。

本页当前需要修正的体验问题：

- **Long table without decision hierarchy（major）**：所有目录行同权，核心商品没有发现优先级；
- **Tabular data without task-level sorting（major）**：数字对齐存在，但用户不能按渠道、价格、更新时间排序；
- **State wording friction（major）**：`0 / 暂无更新` 是系统字段，不是用户语言；
- **Mobile return cost（major）**：390px 全页约 12,096px，高频任务需要分批加载；
- **Duplicate choice ambiguity（major）**：同名条目没有规格说明，买家不知道差别。

本轮不重新设计全站，不引入新主题，不添加装饰图，也不牺牲现有移动端和日夜主题。

## 7. 机会与赚钱路径

排序修复不是纯视觉工作，它直接缩短以下路径：

1. 买家先看到高覆盖核心商品；
2. 打开详情核验价格、更新时间、渠道和售后；
3. 卖家从高覆盖商品判断竞争强度；
4. 进入利润计算器输入自己的采购价、售价、费率和售后准备金；
5. 再从账号商机日报判断是否值得进入，而不是先翻几十行空数据。

后续只有在取得真实点击、收藏、询价或成交事件后，才能把“推荐”升级成个性化或真实热度排序。

## 8. 本轮验收指标

- 默认前 20 行无“暂无在售”商品；
- 默认第 1 行为有在售渠道的核心 AI 商品；
- ChatGPT Plus 主市场条目进入前 3；
- 用户可切换推荐、渠道最多、价格最低、最近更新；
- 用户可筛选全部、有在售、暂无在售；
- `0` 渠道改为“暂无在售”，空时间改为“待首次采集”；
- 390px 初始页面不再渲染全部 71 行，但完整目录和加载更多始终可达；
- 搜索、平台 hash、排序与状态组合工作；
- 不删除任何商品、报价、slug、详情页或后台能力。
