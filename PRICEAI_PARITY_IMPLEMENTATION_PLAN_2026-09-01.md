# 爱窝啦·货源雷达对齐 PriceAI 的实施方案

> 日期：2026-09-01（Asia/Shanghai）  
> 原则：稳定性优先；不删除商品和报价；不复制竞品品牌资产；每阶段可独立回滚。

## 阶段 0：保护基线

1. 从最新 `origin/main` 建立独立 worktree 和 `codex/priceai-parity` 分支。
2. 保存 PriceAI 公开页面与爱窝啦生产基线。
3. 记录当前数据规模、路由状态、桌面/手机截图和 1101 证据。
4. 不修改 AI 日报前后端，货源站失败继续与日报主体隔离。

验收：原目录零覆盖；工作分支只包含本任务文件。

## 阶段 1：P0 稳定性修复

### 1.1 统一安全数据读取

- 给 `catalog-summary` 增加顶层 try/catch 和超时；
- 商品摘要 RPC 失败返回 `null`，不让 metadata 和页面同时失败；
- 商品报价首屏失败返回空结果和站内提示；
- 全部报价目录查询失败返回空筛选数据；
- 首页、渠道、官方价和商机使用安全默认值；
- API 失败返回 JSON 503，不抛到 Worker 形成 1101。

### 1.2 页面内降级

- 保留标题、canonical、解释和重试入口；
- 明确写“货源数据暂时不可用”，不显示 0 元或伪造库存；
- 快照 API、静态指南和商机日报不互相阻塞。

### 1.3 回归测试

- 模拟 RPC、目录和报价请求失败；
- 断言函数返回安全结果；
- 断言元数据生成不抛异常；
- 断言 API 使用受控状态码。

验收：本地与部署后核心路由连续三轮为 200；API 失败为结构化 503。

## 阶段 2：全局导航和设计系统

### 2.1 设计 token

- 主状态色改为爱窝啦深绿；
- 背景改为温暖浅灰和白色表面；
- 新增衬线标题字体栈，只使用本地系统字体；
- 圆角统一为 12/16px 和胶囊；
- 同步暗色 token。

### 2.2 Header

- 可关闭公告条；
- 桌面居中胶囊导航；
- 一级导航改为首页、订阅货源、官方价格、商家经营、指南；
- 渠道、异动、利润、商机进入商家经营菜单或更多菜单；
- 保留主题切换、批发合作和渠道提交。

### 2.3 Mobile

- 移除固定底部导航；
- 添加汉堡菜单抽屉；
- Escape/遮罩/关闭按钮可关闭，焦点不丢失；
- 页面不再预留底栏高度。

### 2.4 Footer

- 按买家、卖家、关于与数据分组；
- 主站链接保持最多 0–2 个；
- 明示信息聚合和交易边界。

验收：320/390/768/1440 无溢出；导航不换行；日夜模式通过。

## 阶段 3：首页改为购买路径入口

1. H1 改为“AI 账号货源、官方价格与商家经营入口”。
2. 主说明解释站点覆盖的购买路径。
3. 三张任务卡：
   - 我只想买一个 AI 订阅；
   - 我想找低价货源或比较渠道；
   - 我想做 AI 账号生意。
4. 四个模块：第三方货源、官方价格、实时异动、经营日报/利润。
5. 数据规模改为小型信任徽章。
6. 保留核验边界、FAQ 和提交入口，但下移。

验收：桌面第一屏完整看到三条路径；手机首条路径在 700px 内。

## 阶段 4：标准商品目录

### 4.1 页面头

- 标题、最近更新时间、商品总数和规则同区显示；
- 统计改为紧凑徽章；
- 增加买前指南链接。

### 4.2 筛选工作区

- 搜索始终第一；
- 类别顺序固定 ChatGPT → Claude → Gemini → Grok → AI 编程 → 其他；
- 视图切换：标准商品、全部报价、渠道商；
- 报价状态与排序进入同一行；
- 手机使用横向滚动标签和筛选按钮。

### 4.3 桌面表格

- 商品、平台、类型/摘要、最低价、质保价、可买报价、渠道/来源、最近更新、操作；
- 数字使用 tabular nums；
- 有货绿色、缺货红色、无证据灰色；
- 无报价商品沉底并有分隔说明。

### 4.4 手机商品卡

- 标题和状态；
- 最低价、来源和更新时间；
- 质保、可买数、渠道数；
- 明确详情箭头；
- 不突出目录编号。

验收：ChatGPT 第一个；Plus 核心商品首屏可达；无货不插在有货中间。

## 阶段 5：商品详情与全部报价

1. 标题前增加平台、类型和规格标签。
2. 报价总数、有货数、排序规则和最近更新合并为一行。
3. 增加库存 ≥50、1 小时内更新及经过验证的语义快捷筛选。
4. 保留关键词、价格区间和可买/缺货高级筛选。
5. 桌面报价表对齐库存、渠道、原名、价格、更新时间、证据、购买、反馈。
6. 手机报价卡按渠道 → 商品 → 库存/价格 → 操作排序。
7. 增加“带入利润”“相关商机”“官方价格”入口。
8. 第二阶段再做同名报价聚合，不能丢原始 URL。

验收：详情首屏完成是否可买、从谁买、多少钱、多新鲜四项判断。

## 阶段 6：渠道、官方价与商家经营

### 渠道

- 搜索、类型和新鲜度筛选；
- 覆盖商品、可买报价、最近成功和主要分类；
- 进入全部报价时自动带上渠道筛选。

### 官方价

- 与第三方商品双向链接；
- 展示地区、周期、支付和税费核验项；
- 无数据时保留应用入口和解释，不返回 1101。

### 商家经营

- 把经营日报、异动和利润放在同一入口；
- 商品详情可带入利润计算器；
- 商机推荐必须来自实时货源。

验收：买家与卖家都能在三次点击内完成主任务。

## 阶段 7：批发合作与商业化

1. 新建 `/wholesale`：批量需求/源头供给双入口。
2. 复用安全投稿动作，增加类型、标题、联系方式和证明 URL。
3. 新建 `/commercial`：广告位置、受众、素材、落地页和审核边界。
4. 广告必须标注，不参与自然排序和最低价计算。

验收：提交成功有状态反馈；联系方式不公开；滥用校验和限速保持。

## 阶段 8：指南和 SEO

- 重组快速入门、价格差异、渠道核验、官方订阅、支付方式、ChatGPT 获取方式、利润和售后；
- 页面之间加入面包屑和下一步；
- 保留 canonical、robots、sitemap、FAQ、Breadcrumb、Organization、WebSite；
- 搜索和筛选参数不生成重复索引。

## 阶段 9：测试、发布和回滚

### 测试

- `pnpm test`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm build`
- `pnpm build:cf:production`
- 根项目 lint/typecheck/test/content/build
- 生产数据只读审计
- 320/390/768/1440，日间/夜间视觉检查
- 核心路由三轮 HTTP/DOM 探测

### 发布

1. 先推任务分支，等待两套 CI；
2. 部署预览并审计；
3. 合并/推送 main；
4. 使用现有 Cloudflare 部署流程；
5. 线上验证后再关闭旧版本回滚入口。

### 回滚

- 所有阶段使用独立提交；
- UI 和稳定性拆开提交；
- 发生 1101、数据丢失、详情链接失败或主体日报受影响时立即 `git revert` 对应提交；
- 不使用 reset、rebase 或覆盖用户目录。

## 本轮实施范围

本轮直接完成阶段 1–5 的核心部分，并完成阶段 6 的入口整合；批发/商业合作先建立可用页面和安全入口，后续真实成交归因与渠道健康评分需要新的业务数据，不能伪造。
