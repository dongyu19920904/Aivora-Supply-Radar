# 爱窝啦·货源雷达修改与优化实施方案

> 制定日期：2026-08-30（Asia/Shanghai）
> 输入：`SUPPLY_RADAR_COMPETITIVE_AUDIT_REPORT.md`
> 原则：主体数据和 AI 日报隔离优先；不删商品、不弱化来源验证；小批次、可测试、可回滚。

## 1. 产品定位

货源雷达不做另一个“报价链接大全”，而做 AI 账号买卖双方的决策工具：

```text
真实市场数据
→ 看懂价格、库存、新鲜度和来源
→ 把日报信号关联到具体商品
→ 计算可售价格和真实利润
→ 找到可核验货源或提交采购需求
→ 用反馈、异动和社区继续校正
```

三类首要用户：

1. 买家：找可靠低价、理解交付和售后风险；
2. 卖家：找供给、看行情、算利润、判断是否上架；
3. 渠道商：被收录、获得买家线索、处理反馈、参与批发合作。

## 2. 目标信息架构

```text
首页 (/)
├── 行情 (/products，规范入口)
│   ├── 全部报价 (/offers)
│   └── 商品详情 (/products/{slug})
├── 渠道 (/merchants)
│   └── 渠道详情 (/merchants/{id-or-slug})
├── 信号
│   ├── 价格异动 (/changes)
│   └── 账号商机 (/opportunities/{date})
├── 工具
│   ├── 利润计算器 (/tools/profit-calculator)
│   ├── 官方价格 (/official-prices)
│   └── 商品对比 (/compare)
├── 社区 (/community)
│   ├── 货源 (/community/supply)
│   ├── 求购 (/community/wanted)
│   ├── 优惠 (/community/deals)
│   ├── 问答 (/community/qa)
│   └── 避坑 (/community/risk)
├── 合作
│   ├── 提交渠道 (/submit)
│   ├── 批发供需 (/wholesale)
│   └── 商业合作 (/commercial)
└── 指南与方法 (/guides、/methodology)
```

第一批不强制物理迁移所有目录；先让新入口和 canonical 策略稳定，再用 301 分批切换，避免一次移动大量动态路由。

## 3. 分阶段实施

## Phase A：P0 数据与安全修复

### A1. 恢复官方地区价格

**文件**

- 复用并验证现有 `v2-web/scraper/`（来自固定授权源码），新增实际调用它的隔离工作流；
- 新增/修改 `.github/workflows/v2-official-prices.yml`；
- 修改 `v2-web/src/lib/official-price-data.ts`；
- 修改官方价格页面和卡片；
- 增加 Python 解析测试和运行摘要校验。

**步骤**

1. 对照授权 SHA 复制采集器、requirements 和测试；
2. 删除上游品牌/域名，只保留官方 App Store、汇率和 Supabase 写入逻辑；
3. local-only 指定 1 个应用、2 个地区 dry-run；
4. 检查 HTTP、最终 URL、币种、金额、套餐映射、未知套餐和失败结果；
5. 扩到完整 3 应用 × 32 地区 dry-run；
6. 只有全量校验通过才允许写预览/生产 Supabase；
7. 页面显示数据时间、地区数和失败时的 last-good，不再静默展示空卡。

**门槛**

- 新套餐无法映射时阻止该地区写入；
- 采集失败不删除旧数据；
- 官方页面至少 3 个应用、10 个有效套餐、20 个地区样本；
- 不使用竞品页面数字冒充官方数据。

### A2. 建立 PriceAI 异动生成

**文件**

- 修改 `v2-web/scripts/import-priceai-snapshot.ts`；
- 修改/新增 `priceai-import` 和事件测试；
- 视需要新增迁移，为事件唯一键和查询建立索引；
- 修改 `/changes` 展示。

**规则**

- 新报价只建基线，不产生异动；
- 同一来源身份 + 同一原商品标题才比较；
- 价格两边都为有效数字且不同才产生涨跌；
- 库存仅在 `in_stock/out_of_stock/offline` 确认变化时产生事件；
- 同一观察时间重复同步不得重复写；
- 来源失败和 stale 保留不得产生假下架；
- 页面区分降价、涨价、补货、缺货、下架，并显示原始证据。

### A3. 投稿安全与 SEO

- 服务端校验 HTTPS、公网主机、字段长度和控制字符；
- 增加蜜罐、重复抑制和速率限制；
- 不在错误中回显数据库细节；
- `/submit` 原地表单、有 loading/success/error 状态；
- 增加 canonical 和 Breadcrumb schema；
- 后台仍使用 service role，浏览器永不接触密钥。

## Phase B：P1 界面与核心任务流

### B1. 设计系统与全局导航

选择 **Workbench** 数据工作台结构，技术/务实语气：

- 首页首屏以真实市场脉搏、搜索和两条任务路径开场，不用大段居中营销词；
- 去除渐变标题、径向装饰、等宽图标功能卡和全站 GitHub 悬浮干扰；
- 使用衬线展示字 + 中文系统无衬线正文字体配对，不再加载 Inter；
- 颜色统一为纸面、墨色、爱窝啦绿、证据蓝、警告琥珀、危险红；
- 所有价格和数量使用 tabular numbers；
- 桌面主导航最多 6 项，移动底栏为“行情、异动、商机、工具、更多”；
- 日夜主题使用明确 token/variant，不再依赖全局类名包含匹配 `!important`；
- 保留现有路由和所有功能，不删除页面。

### B2. 首页改为数据工作台

首屏内容：

- 全站搜索；
- 标准商品数、有报价商品数、有效报价数、活跃来源数和最后同步；
- “我是买家”和“我是卖家”两条明确路径；
- 今日市场脉搏：最新降价/补货、热门商品、最新商机；
- 数据口径和失败隔离简述；
- 渠道商提交/批发合作为次级 CTA。

禁止编造成交、利润或用户量数字。

### B3. 标准商品与全部报价

- 无报价使用 `— / 暂无在售报价`，不显示 `¥0.00`；
- 默认把有报价且最近更新的商品放前；
- 统一一张可排序主表/移动卡，不再分 11 张重复表；
- 筛选支持平台、类别、库存、价格、更新时间、来源类型和质保；
- 移动端使用筛选抽屉并显示已选条件；
- URL 保存搜索、筛选、排序和页码；
- 全部报价 API 保持每页上限 100，不恢复全量返回。

### B4. 商品详情决策层

详情头部显示：

- 在售最低价；
- 明确质保最低价（没有可靠标签则“未知”）；
- 有货/缺货/全部渠道数；
- 中位价和合理价格区间；
- 最近成功采集时间；
- 与上一有效快照的变化。

增加：库存/更新时间/来源/质保筛选、排序、对比选择、异常低价提示、相关商机、利润计算器入口和相关指南。

### B5. 渠道列表与详情

- 服务端分页，每页 30–50；
- 可按名称、系统、平台、活跃状态和最近成功筛选；
- 列表显示有效报价数、商品数、最近成功、来源状态和抓取类型；
- 详情增加价格竞争力、商品覆盖、最近成功/失败和用户反馈；
- 公开“收录”不等于认证或担保，认证/赞助不改变自然排序。

## Phase C：卖家赚钱闭环

### C1. 利润计算器（第一批可无登录）

路径：`/tools/profit-calculator`

输入：

- 采购成本；
- 计划售价；
- 平台费率；
- 支付费率；
- 固定交付成本；
- 售后/退款准备金；
- 预计销量。

输出：

- 单笔毛利、净利和净利率；
- 保本售价；
- 预计总净利；
- 成本上升 5%/10% 的敏感性；
- 结果说明和风险提示。

计算全在浏览器完成，不保存账号或交易隐私；可从商品详情携带当前报价作为成本，但用户必须确认规格和质保一致。

### C2. 商机与货源关联

- 规则提取日报中明确出现的商品名/slug；
- 未达到明确匹配阈值不自动关联；
- 商机详情显示关联商品的最低价、渠道数、更新时间和利润计算入口；
- 继续保留日报原文、证据链接和停止条件；
- 不增加模型调用，不修改 AI 日报生产提示词。

### C3. 批发供需

- `/wholesale` 区分“我有批量需求”和“我有源头供给”；
- 表单包含方向、数量、目标价、结算、来源证明、测试和联系方式；
- 默认人工审核，不承诺撮合或担保；
- 合格线索可与标准商品/渠道关联；
- 建立后台审核列表和状态，不把联系方式公开。

## Phase D：审核型社区与避坑

- 数据表：帖子、回复、分类、关联商品/渠道、证据、审核和更正；
- 公开类别：货源、求购、优惠、问答、避坑；
- 避坑必须区分“用户陈述、证据待核、已核实、已更正”；
- 被提及方有提交反证和更正入口；
- 社区报价不进入自然价格排序，审核后才能成为采集候选；
- 先做只读列表和投稿审核，再开放回复/登录，避免一次引入完整论坛系统。

## Phase E：增长与商业化

- `/commercial`：公开赞助位置、素材、标记、审核和自然排序边界；
- 商家认证资料页，但认证不等于交易担保；
- 收藏/降价/补货提醒：先匿名浏览器本地收藏，再评估账户和通知；
- 出站点击只收集匿名事件，不收集交易隐私；
- 指南扩展到异常低价、质保、来源健康、利润、售后和批发；
- API/CSV 只在数据授权、限速、字段和定价明确后开放。

## 4. 本轮实施范围

本轮优先交付能安全闭环的首批改动：

1. 官方价格采集器复核、local-only dry-run 和工作流启用；
2. PriceAI 价格/库存异动生成与回归测试；
3. 商品无报价不再显示 `¥0.00`；
4. 投稿输入安全、canonical 和可访问状态；
5. Workbench 首页、导航、移动导航和页脚；
6. 利润计算器；
7. 商机页加入关联货源/利润入口的第一版；
8. 渠道分页/摘要和移动筛选的可安全部分；
9. 更新视觉和竞品审计回归。

社区完整数据模型、登录/回复、商业合作后台、收藏提醒和全 URL 迁移属于后续独立发布；本轮会保留入口和稳定契约，不用静态假内容冒充已完成功能。

## 5. 预计修改文件

### 文档与审计

- `SUPPLY_RADAR_COMPETITIVE_AUDIT_PLAN.md`
- `SUPPLY_RADAR_COMPETITIVE_AUDIT_REPORT.md`
- `SUPPLY_RADAR_OPTIMIZATION_PLAN.md`
- `scripts/competitive-site-audit.ts`

### 数据与同步

- `v2-web/scripts/import-priceai-snapshot.ts`
- `v2-web/src/lib/priceai-import.ts`
- 相关 `.test.ts`
- `v2-web/scraper/**`
- `.github/workflows/v2-official-prices.yml`
- 必要 Supabase migration

### 页面与组件

- `v2-web/src/app/page.tsx`
- `v2-web/src/app/globals.css`
- `v2-web/src/app/tokens.css`
- `v2-web/src/components/Header.tsx`
- `v2-web/src/components/MobileNav.tsx`
- `v2-web/src/components/Footer.tsx`
- `v2-web/src/components/FloatingGithubBanner.tsx` 或从全局布局移除引用
- 商品、商机、异动、投稿和工具页面的最小关联文件
- `.hallmark/preflight.json`、`.hallmark/log.json`

不计划删除路由树、后台、数据库表或授权资产。

## 6. 测试矩阵

1. Root：Biome、TypeScript、Vitest、内容检查、seed dry-run、Wrangler dry-run；
2. V2：Node tests、TypeScript、ESLint、Next build、OpenNext Linux build；
3. Python：解析、币种、套餐映射、last-good、未知套餐、数据库写入顺序；
4. 数据：PriceAI exact count、无 HTTPS/孤儿/重复、异动首次基线和重复抑制；
5. 页面：状态、文本、canonical、schema、坏图、控制台、320/375/390/414/768/1440；
6. 主题：日间、夜间、系统主题和 reduced motion；
7. 任务流：买家比价、卖家算利润、渠道投稿；
8. 安全：投稿 URL、长度、蜜罐、重复、限速、后台匿名访问；
9. 独立性：AI 日报调用/触发为 0。

## 7. 发布顺序

1. 本地单元测试和 dry-run；
2. Python 官方价格 local-only，不写生产；
3. 提交任务分支并等待 V1/V2 CI；
4. 精确 SHA 部署预览 Worker；
5. 预览执行桌面/手机、日/夜和真实数据审计；
6. 官方价格与异动写入先在可回滚数据库事务/幂等任务中验证；
7. 精确 SHA 生产部署一次；
8. 生产验证 URL、数据、SEO、移动和缓存；
9. 观察同步任务，确认未重复生成异动；
10. 记录运行、耗时、Cloudflare 体积和回滚 SHA。

## 8. 回滚

- 前端：重新部署上一 V2 代码 `64e87811f315db77efb198fcfa2d71f1be628d33`；
- 整站：Pages edge binding 恢复 V1 Worker；
- 官方价格：新任务失败即停止，保留原表 last-good，不执行清空；
- 异动：事件写入幂等，可按本次导入时间删除本批事件而不改当前报价；
- 数据迁移：只新增表/索引/约束，避免破坏性列删除；
- AI 日报：本轮不修改、不触发，因此无需日报回滚。

## 9. 资源与调用变化预估

- 首页/渠道分页会减少 Worker HTML 和数据库读取；
- 官方价格任务新增每日一次 GitHub Actions + Supabase 写入，不占 Cloudflare Cron；
- 异动比较在现有 30 分钟 PriceAI 同步内增加一次约 5,000 行旧值读取和少量事件写入；
- 利润计算器纯浏览器计算，Cloudflare 和模型调用为 0；
- 商机关联第一版使用确定性规则和现有数据，模型调用为 0；
- 生产部署 Worker 包体变化在构建后记录，目标不增加大型客户端依赖。

## 10. 完成标准

- 官方价格不再是空壳，且显示可核验来源和更新时间；
- 下一次真实价格/库存变化能形成一次且仅一次事件；
- 无报价商品不再显示 `¥0.00`；
- 卖家能从商机或商品进入利润计算器得到真实可解释结果；
- 首页、导航和移动端不再呈现通用 AI 模板；
- 投稿不能写入非 HTTPS/超长/明显重复内容；
- 完整测试、预览、生产和线上验证通过；
- AI 日报生产触发与模型调用为 0；
- 所有未在本轮完成的 P2/P3 项以真实状态记录，不用静态假模块冒充完成。

## 11. 本轮实施记录（2026-08-30）

已完成首批 P0/P1：

- 为既有 App Store 采集器补齐独立的每日工作流，没有把失败传播到主货源同步；
- PriceAI 导入先读取上一快照，只在连续、前进时间点上的真实价格或库存变化时写入异动；
- 修正无报价商品的 `¥0.00` 伪价格和“最高报价等于质保价”的错误语义；
- 渠道页优先使用数据库聚合 RPC，降级路径也限制读取量；真实数据预览发现 380 行导致手机页面过长后，又增加 40 条/页的搜索后分页，完整数据仍可检索；
- 投稿增加 HTTPS、私网/本机、凭据、端口、长度、蜜罐和重复校验；
- 首页、导航、移动导航和页脚改为货源工作台结构，保留日夜主题；
- 新增纯前端利润计算器，并从账号商机列表和详情连接到货源与利润动作；
- 增加 320、390、768、1280 px 的发布视觉审计，覆盖日夜主题、canonical、单一 H1、横向溢出、按钮文字换行、坏图和控制台错误。

本地验收结果：Node 单元测试 28/28、Python 官方价格解析测试 23/23、Root/V2 TypeScript、V2 Next 生产构建、Supabase migration dry-run 均通过；V2 ESLint 为 0 error、101 个继承 warning；20 个关键页面/视口/主题视觉用例全部通过。Windows 上的 OpenNext 本地打包因其官方不支持的符号链接行为出现 `EPERM`，因此 OpenNext 产物以 GitHub Linux CI 和预览 Worker 为发布门禁。

生产前的单次官方价真实采集又发现 Apple 新增 `500 Credits`、`1000 Credits`，旧规则只识别 `100 Credits`，因此安全拒绝整批写入。已将 ChatGPT 纯数字 Credits 识别为可展示点数包，并保留对 `Unlimited Credits` 等未知名称的阻断回归测试；本轮不重复触发生产采集，由下一次独立日程验证。

仍按原计划留到后续独立发布：真实社区账户/帖子/回复、收藏提醒、商业合作后台、完整 URL 迁移和大规模性能压测。本轮不以静态内容冒充这些能力。
