# 爱窝啦·货源雷达分类与账号商机修改方案

日期：2026-08-31（Asia/Shanghai）
关联检查报告：`SUPPLY_RADAR_CATALOG_OPPORTUNITY_AUDIT_2026-08-31.md`

## 1. 实施边界

- 只修改 `Aivora-Supply-Radar` V2 前台、确定性数据组合逻辑和测试。
- 不修改 AI 日报前后端，不触发 AI 日报生产任务，不新增大模型调用。
- 不删除或合并现有标准商品，不篡改原始平台、价格、库存、来源 URL 和时间戳。
- 不新增数据库迁移；复用 `get_product_catalog_summary()`、`market_price_changes` 和 `account_opportunities` 的公开只读字段。
- 任一辅助数据读取失败独立降级，商品目录主体优先。

## 2. Phase A：建立统一用户侧分类法

新增 `src/lib/catalog-taxonomy.ts`：

1. 定义固定分类 ID、名称、短说明和优先级。
2. `classifyCatalogProduct(product)` 根据明确商品名和原始平台归类。
3. `groupCatalogProducts(products)` 按固定顺序分组，空组不渲染。
4. `getCatalogCategoryOptions(products)` 为下拉和快捷导航提供同一来源的数据。
5. 保留不可识别商品到“其他”，禁止丢弃。

回归测试：

- ChatGPT 必须排第一。
- Cursor/Kiro/Windsurf 归 AI 编程。
- Perplexity/Suno/Dreamina 归 AI 创作与效率。
- 邮箱、接码/KYC、社媒、API/卡券分别归类。
- 输入总数等于所有分组商品数，任何商品只出现一次。

## 3. Phase B：把目录长表改成分类目录

修改 `CardProductsClient.tsx`：

1. 将 `selectedPlatform` 改成 `selectedCategory`，URL hash 使用稳定分类 ID。
2. 下拉选项读取统一分类法，显示分类商品数量，并按 ChatGPT → Claude → Gemini → Grok → 其他业务分类排列。
3. 增加横向可滚动的热门分类快捷导航，便于移动端单击定位/筛选。
4. 搜索、库存状态和排序先作用于全部商品，再按分类分组。
5. 默认显示所有分类，不再用 24 项全局截断隐藏后续分类。
6. 显示“匹配商品数 / 分类数”，并说明排序只作用于分类内部。

修改 `MasterTable.tsx`：

1. 支持分类标题外部提供起始序号，避免每组都从 01 重新编号。
2. 保留缺货分隔、移动端卡片、详情入口和当前库存语义。

新增分类分区组件：

- 分类标题、用途说明、商品数、可购买数、可购买报价数。
- 稳定 `id="category-<id>"` 和诊断 `data-catalog-category` 属性。
- 同类商品连续渲染，分类之间有明确视觉分隔。

## 4. Phase C：建立实时货源商机模型

新增 `src/lib/supply-opportunity.ts`，只做纯函数：

1. 读取分类后的目录摘要和最近价格/库存变化。
2. 去重同一商品/渠道的重复变化。
3. 识别以下线索：
   - `restock`：库存从不可购买变为可购买；
   - `stockout`：库存从可购买变为不可购买；
   - `price_drop`：有效前后价下降且幅度达到阈值；
   - `price_rise`：有效前后价上涨且幅度达到阈值；
   - `supply_gap`：热门分类商品当前可购买渠道较少；
   - `crowded`：渠道极多，仅作为红海风险提示。
4. 每条线索生成确定性的证据说明、买家动作、卖家动作、停止条件和优先级。
5. 不使用销量、利润或需求已验证等无法从数据证明的措辞。
6. 生成平台供给地图：分类商品数、可购买商品数、可购买报价数、分类最低价。

回归测试覆盖价格百分比、库存状态、低供给/拥挤阈值、分类优先级和无数据降级。

## 5. Phase D：重做账号商机页面

修改 `/opportunities`：

1. 并行读取目录摘要、最近异动和旧日报。
2. 首屏改为“今日货源机会与风险”，显示实时盘面时间和四项指标。
3. 主体展示 4–6 条可执行货源线索，每条关联具体商品。
4. 增加平台供给地图，帮助卖家选择供给不足或竞争拥挤的分类。
5. 增加“货源详情 → 利润计算 → 停止条件”的操作闭环。
6. 将旧日报移动到“行业信号参考”区域，明确它不是当前库存结论。

修改 `/opportunities/[date]`：

1. 保留旧日报正文和公开证据。
2. 根据报告文本匹配当前标准商品和平台分类。
3. 在正文前显示“与当前货源的关联”：当前最低价、可购买渠道、更新时间和商品详情入口。
4. 无匹配时明确提示先返回实时商机台，不伪造关联。

## 6. Phase E：审计与视觉回归

扩展 `scripts/visual-audit.ts`：

1. 断言商品目录第一个分类为 ChatGPT。
2. 断言分类 ID 唯一、分类顺序稳定、所有可见商品只属于一个分类。
3. 打开平台下拉，断言第一个业务选项是 ChatGPT，且不存在“编程工具”排在前面。
4. 断言账号商机页存在实时盘面、具体商品链接、供给地图和旧日报辅助区。
5. 保留桌面/手机、日间/夜间、库存状态、破图、溢出和控制台错误检查。

## 7. Phase F：验证与发布

依次执行：

1. `pnpm lint`
2. `pnpm typecheck`
3. `pnpm test`
4. `pnpm audit:data`（生产只读）
5. `pnpm import:priceai -- --dry-run`（完整源快照，数据库写入 0）
6. `pnpm build`
7. GitHub/Linux `build:cf`、preview bundle 和 production bundle
8. 部署精确提交到 V2 preview
9. 预览桌面/390px 手机、日间/夜间视觉验收
10. SSH 推送 `main` 后，通过既有后端工作流部署相同精确 SHA
11. 验证正式域名的分类顺序、商品总数、商机实时指标、canonical、schema、sitemap 和 Worker 直连

## 8. 发布门槛

- 分类测试、商机纯函数测试、完整测试或生产构建任一失败，不部署。
- 目录总数减少、商品跨分类重复、ChatGPT 不是首分类、商机使用无证据收益承诺，均视为阻断。
- 预览和生产必须使用同一个精确 SHA。
- 不触发 AI 日报；货源数据同步仅在代码部署后确有必要时执行一次。

## 9. 回滚

- 代码使用 `git revert <本轮提交>`，不 reset、不 rebase。
- Cloudflare Pages 边缘绑定可切回发布前 V2 Worker version，或现有 V1 回滚服务。
- 本轮不迁移数据库，因此代码回滚不需要数据回滚。

## 10. 后续迭代（不阻塞本轮）

1. 同名标准商品聚合为“主商品 + 多来源目录”，展开查看来源，避免视觉重复但不丢报价。
2. 增加库存 ≥10/50、1 小时内更新、质保和风险等级筛选。
3. 为卖家提供补货、涨价、降价订阅和分类级库存稳定率。
4. 接入匿名点击和利润计算完成事件后，再用真实行为校准商机排序，不能用渠道数冒充销量。
